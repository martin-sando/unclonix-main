server {
    server_name verify.unclonix.com;
    client_max_body_size 16m;

    location / {
        include proxy_params;
        proxy_read_timeout 3000s;
        proxy_connect_timeout 3000s;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass http://0.0.0.0:2103;
    }
}
